# you describe your data using the YAML notation here
# and then load them using Fixtures.load("data.yml")

User(admin):
     email: admin@gmail.com
     password: admin
     fullname: admin

Product(p1):
  category : PC;
  model : PAVILON;
  brand : HP;
  price : 1574;
  #Para comentar , aqui poneis la url de los productos (la imagen)